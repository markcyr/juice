<div class="nested-fields">

  <div class="field ingredient">
  <%= f.label :ingredient_id%>
  <%=f.collection_select :ingredient_id, Ingredient.all, :id, :name %>
  </div>

  <div class="field">
  <%= f.label :quantity%>
  <%=f.number_field :quantity%>
  </div>

  <div class="field">
  <%= f.label :unit_id%>
  <%#=f.collection_select :ingredient_id, Ingredient.all, :id, :measure %>
  <%= f.collection_select :unit_id, Unit.all, :id, :unit_name %>
  </div>

  <!-- <div id="ingredients"> -->

    <%#=f.fields_for :ingredient do |ingredient|%>
      <%#= render 'ingredient_field', ingredient: ingredient%>
    <%#end%>
  <!-- </div> -->
</div>
